git status
git add -A
(You should see "Changes to be committed" in green)
git commit -m "Add real-time photo sync and fix date sorting"
git commit -m "Update sidebars and implement delivery filtering"
git commit -m "Add backend endpoints for photo management"
git push origin main
git statuz
